import { Link } from 'react-router-dom';

const Landing = () => {

    return (
        <div>

            <Link to="/home">
                <button>Home</button>
            </Link>

        </div>
    );
};

export default Landing;